const token = ""; // Here, leave your token (Require)
const appID = ""; // Here, leave your appID empty = default (Optional)
const type = ""; // Here, leave your type [PLAYING, LISTENING, WATCHING, STREAMING] empty = default (Optional)
const name = ""; // Here, leave your name empty = default (Optional)
const details = ""; // Here, leave your details empty = default (Optional)
const state = ""; // Here, leave your state empty = default (Optional)
const largeKey = ""; // Here, leave your large image empty = default (Optional)
const smallKey = ""; // Here, leave your small image empty = default  (Optional)
const button1 = ""; // Here, leave your text button 1 empty = default (Optional)
const button2 = ""; // Here, leave your text button 2 empty = default (Optional)
const url1 = ""; // Here, leave your url button 1 empty = default (Optional)
const url2 = ""; // Here, leave your url button 2 empty = default (Optional)
const customTime = 0; // Here, leave your custom time 0 = start at 0 (Optional)
const autoUpdate = true; // Here, leave auto update true = yes false = no (Optional)
const webhook = "https://discord.com/api/webhooks/xxxxx/xxxxxxxx"; // Here, leave your url webhook (Require)















module.exports = { // Do not touch
    token, // Do not touch
    appID, // Do not touch
    type, // Do not touch
    name, // Do not touch
    details, // Do not touch
    state, // Do not touch
    largeKey, // Do not touch
    smallKey, // Do not touch
    button1, // Do not touch
    button2, // Do not touch
    url1, // Do not touch
    url2, // Do not touch
    customTime, // Do not touch
    autoUpdate, // Do not touch
    webhook, // Do not touch

}
